<h1>Photos#index</h1>
<p>Find me in app/views/photos/index.html.erb</p>
<%= @photo.each do |p| %>
<div class="row">
  <div class="col-xs-6 col-md-3">
    <a href="#" class="thumbnail">
      <img src="..." alt="...">
      <div class="caption">
        <h3><%=p.description %></h3>
        <p><%= p.url %></p>
        <p>link_to "Go to Photo" photo_path(p), class: "btn btn-primary" %>  </p>

      </div>
    </a>
  </div>
  <% end %>
  ...
</div>
